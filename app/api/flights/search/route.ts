import { NextResponse } from "next/server";
import { searchFlights } from "../../../../lib/flights/search";
export async function POST(request:Request){try{const body=await request.json();if(!body.origin||!body.destination||!body.departureDate)return NextResponse.json({error:"Origin, destination and departure date are required."},{status:400});return NextResponse.json(await searchFlights(body));}catch(e:any){return NextResponse.json({error:e?.message||"Unable to search flights right now."},{status:500});}}
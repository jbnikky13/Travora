"use client";
import { useState } from "react";
export default function AuthPage() {
 const [mode,setMode]=useState<"signin"|"signup">("signin"); const [message,setMessage]=useState("");
 return <main className="authPage"><div className="authCard"><a className="brand authBrand" href="/">Trav<span>ora</span></a><h1>{mode==="signin"?"Welcome back":"Create your Travora account"}</h1><p className="muted">{mode==="signin"?"Sign in to manage your flights and reservations.":"Save passengers, bookings and upcoming flights in one place."}</p><form onSubmit={e=>{e.preventDefault();setMessage("Authentication UI is ready. Connect Supabase Auth environment variables to enable accounts.")}}>{mode==="signup"&&<div className="field authField"><label>Full name</label><input required placeholder="Your full name"/></div>}<div className="field authField"><label>Email</label><input required type="email" placeholder="you@example.com"/></div><div className="field authField"><label>Password</label><input required minLength={8} type="password" placeholder="At least 8 characters"/></div><button className="authSubmit"> {mode==="signin"?"Sign in":"Create account"} </button></form><button className="googleBtn" onClick={()=>setMessage("Google sign-in is prepared for the Supabase Auth integration.")}>Continue with Google</button>{mode==="signin"&&<a className="forgot" href="/auth/reset">Forgot password?</a>}{message&&<div className="authNotice">{message}</div>}<p className="switch">{mode==="signin"?"Don't have an account?":"Already have an account?"} <button onClick={()=>{setMode(mode==="signin"?"signup":"signin");setMessage("")}}>{mode==="signin"?"Create one":"Sign in"}</button></p><a className="backHome" href="/">← Back to Travora</a></div></main>;
}
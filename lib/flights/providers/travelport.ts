import type {FlightSearch,NormalizedFlight} from "../types";
export async function searchTravelport(input:FlightSearch,token:string):Promise<NormalizedFlight[]>{const url=process.env.TRAVELPORT_SEARCH_URL;if(!url)throw new Error("TRAVELPORT_SEARCH_URL is not configured");const r=await fetch(url,{method:"POST",headers:{"Authorization":"Bearer "+token,"Content-Type":"application/json","Accept":"application/json"},body:JSON.stringify(input),cache:"no-store"});const d=await r.json();if(!r.ok)throw new Error("Travelport search failed");const offers=d.offers||d.data?.offers||d.data?.CatalogProductOfferings||[];return offers.map((o:any)=>({id:o.id||o.identifier||crypto.randomUUID(),provider:"travelport",airline:o.airline||o.carrier?.name||"Airline",airlineCode:o.airlineCode||o.carrier?.iata_code,flightNumbers:o.flightNumbers||[],departure:o.departure||"",arrival:o.arrival||"",stops:Number(o.stops||0),currency:o.currency||"USD",price:String(o.price??o.total_amount??""),raw:o}));}